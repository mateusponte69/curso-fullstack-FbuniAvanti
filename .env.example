# ==================== SERVER CONFIG ====================
# Porta do servidor
# Development: 3000
# Production (Render): Render fornece automaticamente via env var PORT (geralmente 10000)
PORT=3000

# URL do frontend para CORS
# Development: http://localhost:5173
# Production (Render): https://seu-app.onrender.com
FRONTEND_URL=http://localhost:5173

# ==================== DATABASE CONFIG ====================
# SQLite (Development)
# DATABASE_URL="file:./dev.db"

# PostgreSQL (Production - Render)
# Obtenha esta URL no painel do Render ao criar um PostgreSQL Database
# IMPORTANTE: Render exige SSL - sempre adicione ?sslmode=require no final
# Formato: postgresql://user:password@host:5432/database?sslmode=require
# Exemplo: postgresql://taskflow_user:abc123@dpg-xyz.oregon-postgres.render.com/taskflow?sslmode=require
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ==================== AUTHENTICATION CONFIG ====================
# Segredo para JWT (mude em produção para um valor forte e aleatório)
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=uma-frase-muito-secreta-e-aleatoria

# ==================== VITE CONFIG (Frontend) ====================
# URL base da API para o frontend consumir
# Development: http://localhost:3000
# Production: mesma URL do backend (o Express serve o frontend)
VITE_BASE_URL=http://localhost:3000
